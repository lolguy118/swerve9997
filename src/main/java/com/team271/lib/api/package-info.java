/**
 * Vendor-neutral hardware interfaces and identifiers (the {@code api} layer).
 *
 * <p>This package is {@link org.jspecify.annotations.NullMarked @NullMarked}: every type usage is
 * non-null by default, and anything that may be {@code null} is tagged {@link
 * org.jspecify.annotations.Nullable @Nullable}. Null-safety is enforced by NullAway at build time
 * (warning during rollout) per ADR-018.
 */
@NullMarked
package com.team271.lib.api;

import org.jspecify.annotations.NullMarked;
